package unit2;

public class Lab_2_1 
{
	public static void main(String args[])
	{
		// SECTION 1
		
		// Use the comparison operators once each to make all of the boolean statements below false and uncomment them once you finish
		boolean A = true, B = true, C = true, D = true, E = true, F = true;
		// A = 4 _ 6;
		// B = 9 == 9
		// C = 3 < 8
		// D = 5 < 6
		// E = 2 == 2
		// F = 8 > 1
		// Print the booleans
		System.out.println("A: " + A +
							"\nB: " + B +
							"\nC: " + C +
							"\nD: " + D +
							"\nE: " + E +
							"\nF: " + F );
		
		// SECTION 2
		
		// Declare 3 boolean variables X, Y, and Z 
		boolean X = true, Y=true, Z=true;
		
		// initialize X, Y, and Z so that the print statement will print true.
		
		// print the boolean expression. Uncomment the print statement after initializing X, Y, and Z
		System.out.println(X && Y || !Z);
		
		
		// SECTION 3
		
		// Declare boolean variables
		boolean G = true, H = false, J = false, K = true;
		// use 3 && and 3 || to make each of the following print true and uncomment them after adding the operator
		// System.out.println(G || J)
		// System.out.println(H || K)
		// System.out.println(G && K)
		// System.out.println(!H && !J)
		// System.out.println(G || H)
		// System.out.println(K && !J)
		
		// SECTION 4
		
		// write an expression using 2 boolean expressions that uses one of the short circuiting shortcuts
		// declare 2 boolean expressions
		
		// print out the short circuit expression
		System.out.println();
	}

}
